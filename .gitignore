# Python
__pycache__/
*.py[cod]
*.pyo
*.egg-info/
dist/
build/
.eggs/

# Ambientes virtuais
venv/
.env/
.venv/
env/

# Jupyter
.ipynb_checkpoints/
*.ipynb_checkpoints

# Modelos grandes (opcional — remova se quiser versionar os .pkl)
# output/models/*.pkl

# IDEs
.vscode/
.idea/
*.swp

# OS
.DS_Store
Thumbs.db

# Dados sensíveis (em produção, dados reais NUNCA vão pro git)
# output/data/*.csv   ← descomente se quiser omitir os dados

# SQLite e fila de mensagens local
output/*.db
output/jobs.db

# MLflow (apêndice notebooks/02 — tracking local regenerável)
mlruns/
mlartifacts/
notebooks/mlruns/
notebooks/mlartifacts/
notebooks/mlflow.db
mlflow.db

# Cache de testes
.pytest_cache/

# Relatorios de drift (monitor.py / orchestrator.py) — regeneraveis, datados
output/monitor/drift_report_*
