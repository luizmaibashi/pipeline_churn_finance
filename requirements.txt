# Versões travadas com == : o projeto serializa Pipelines sklearn em .pkl
# (output/models/*.pkl) e joblib quebra silenciosamente ao carregar um .pkl
# entre versões divergentes de scikit-learn. Os pins abaixo batem com a
# execução de 2026-09-10 que regenerou gb_pipeline_v2.pkl e o SHAP v2 (artefatos
# versionados byte-idênticos, 55 testes verdes). Ver .claude/rules/dados.md da base.
numpy==2.4.4
pandas==2.3.3
scipy==1.17.1
scikit-learn==1.8.0
joblib==1.5.3
plotly==6.7.0
jupyter==1.1.1
ipykernel==7.2.0
streamlit==1.56.0
shap==0.52.0
fastapi==0.136.1
uvicorn[standard]==0.46.0
python-dotenv==1.2.2
pytest==9.0.3

# Opcionais — não exercitados pelos 55 testes nem pelo caminho DEMO do agente:
#   openai            : agent.py FULL MODE (function calling), só com OPENAI_API_KEY
#   python-multipart  : endpoints de formulário/upload do FastAPI (a API só usa JSON hoje)
openai==2.32.0
python-multipart==0.0.31
